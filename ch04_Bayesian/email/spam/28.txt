class vector3: def __init__(self, x_ = 0, y_ = 0, z_ = 0,w_=0): #构造函数  self.x = x_  self.y = y_  self.z = z_   self.w = w_ def __add__(self, obj): #重载+作为加号  return vector3(self.x+obj.x, self.y+obj.y, self.z+obj.z,self.w+obj.w) def __sub__(self, obj): #重载-作为减号  return vector3(self.x-obj.x, self.y-obj.y, self.z-obj.z,self.w-obj.w) def __mlt__(self, obj): #重载*作为点乘  return vector3(self.x*obj.x, self.y*obj.y, self.z*obj.z,self.w*obj.w)  def __mod__(self, obj): #重载*作为点除  return vector3(self.x/obj.x, self.y/obj.y, self.z/obj.z,self.w/obj.w) j.y-obj.x*self.y) def __str__(self): #供print打印的字符串  return str(self.x)+','+str(self.y)+','+str(self.z)v1 = vector3(1, 2, 3,4)v2 = vector3(0, 1, 2,5)print v1 + v2print v1 - v2print v1 * v2print v1 / v2